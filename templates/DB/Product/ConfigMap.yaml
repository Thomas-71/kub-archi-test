apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-product-secret
  labels:
    app: postgres
data:
  POSTGRES_DB: "{{ .Values.postgres.product.dbName }}"
  POSTGRES_USER: "{{ .Values.postgres.product.name }}"
  POSTGRES_PASSWORD: "{{ .Values.postgres.product.password }}"