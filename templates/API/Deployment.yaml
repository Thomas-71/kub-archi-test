apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
  namespace: argo-kube-test
spec:
  replicas: {{ .Values.express.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-api
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-api
    spec:
      containers:
        - name: api-container
          image: "breizhsport-server:local"
          ports:
            - containerPort: {{ .Values.node.podInternalPort }}
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"